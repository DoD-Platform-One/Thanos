objstoreConfig: |-
  type: s3
  config:
    bucket: "thanos"
    endpoint: minio.monitoring.svc.cluster.local
    access_key: "minio"
    secret_key: "minio123"
    insecure: true
minio:
  enabled: true
